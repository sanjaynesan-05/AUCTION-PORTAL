# 🏗️ System Architecture & Integration Diagram

## Overall System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET (HTTPS)                         │
└─────────────────────────────────────────────────────────────────┘
                                 ▲
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
        ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
        │   Browser    │  │   Browser    │  │   Browser    │
        │  (Chrome,    │  │  (Firefox,   │  │   (Safari,   │
        │   Firefox)   │  │    Edge)     │  │    etc)      │
        └────────┬─────┘  └──────┬───────┘  └──────┬───────┘
                 │                │                │
                 └────────────────┼────────────────┘
                                  │
                    ┌─────────────▼──────────────┐
                    │   Frontend Application    │
                    │  (React + Vite + Tailwind)│
                    │ https://...frontend...com │
                    │  ├─ Login Page            │
                    │  ├─ Admin Dashboard      │
                    │  ├─ Presenter Panel      │
                    │  └─ Viewer Screen        │
                    └──────────┬────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        │   HTTP Requests      │   WebSocket (WS)     │
        │   (REST API Calls)   │   (Real-time)        │
        │                      │                      │
        ▼                      ▼                      ▼
┌────────────────────────────────────────────────────────────────┐
│                  Backend Application                            │
│               (FastAPI + Uvicorn + SQLAlchemy)                 │
│         https://auction-portal-7bds.onrender.com               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                  API Router Layer                        │ │
│  │  ┌────────────┐  ┌────────────┐  ┌──────────────────┐  │ │
│  │  │   /auth    │  │ /players   │  │  /teams          │  │ │
│  │  │ - login    │  │ - GET list │  │  - GET list      │  │ │
│  │  │ - logout   │  │ - GET /{id}│  │  - GET /{id}     │  │ │
│  │  │ - profile  │  │ - CREATE   │  │  - PUT /purse    │  │ │
│  │  └────────────┘  └────────────┘  └──────────────────┘  │ │
│  │  ┌────────────┐  ┌──────────────────────────────────┐   │ │
│  │  │ /auction   │  │  /management                     │   │ │
│  │  │ - start    │  │  - reset auction                 │   │ │
│  │  │ - pause    │  │  - manage players                │   │ │
│  │  │ - resume   │  │  - manage teams                  │   │ │
│  │  │ - next     │  │  - admin functions               │   │ │
│  │  │ - bid      │  └──────────────────────────────────┘   │ │
│  │  │ - sold     │                                         │ │
│  │  └────────────┘                                         │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Authentication & Authorization              │ │
│  │  - JWT Token Generation & Validation                     │ │
│  │  - Role-based Access Control (admin/presenter/viewer)    │ │
│  │  - Token Expiry: 24 hours                                │ │
│  │  - Password Hashing: bcrypt                              │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              WebSocket Handler (/ws/auction)             │ │
│  │  - Real-time auction updates to all connected clients    │ │
│  │  - Broadcast on: bid placed, player sold, state change   │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
└────────────────────────────┬─────────────────────────────────┘
                             │
        ┌────────────────────┴────────────────────┐
        │                                         │
        ▼ SQL Queries                             ▼ (Environment)
┌──────────────────────────┐              ┌──────────────────────┐
│  PostgreSQL Database     │              │   Environment Vars   │
│  (Neon Serverless)       │              │                      │
│                          │              │  ├─ ADMIN_PASSWORD   │
│  Tables:                 │              │  ├─ PRESENTER_PWD    │
│  ├─ users (12 rows)      │              │  ├─ TEAM_*_PASSWORD  │
│  │  ├─ id                │              │  ├─ SECRET_KEY       │
│  │  ├─ username          │              │  ├─ DATABASE_URL     │
│  │  ├─ password_hash     │              │  ├─ ALGORITHM        │
│  │  ├─ role              │              │  └─ TOKEN_EXPIRE_MIN │
│  │  ├─ team_id           │              │                      │
│  │  └─ team_name         │              │  🔴 CURRENTLY:       │
│  │                       │              │     NOT SET          │
│  ├─ teams (10 rows)      │              │     (CAUSES 500 ERR) │
│  │  ├─ id                │              │                      │
│  │  ├─ name              │              └──────────────────────┘
│  │  ├─ purse             │
│  │  └─ budget_used       │
│  │                       │
│  ├─ players (53 rows)    │
│  │  ├─ id                │
│  │  ├─ name              │
│  │  ├─ role              │
│  │  ├─ base_price        │
│  │  ├─ status            │
│  │  └─ team_id           │
│  │                       │
│  └─ auction_state       │
│     ├─ id                │
│     ├─ is_started        │
│     ├─ current_player_id │
│     ├─ current_bid       │
│     └─ current_bidder_id │
│                          │
│  Region: ap-southeast-1  │
│  Status: ✅ Connected    │
└──────────────────────────┘
```

---

## Data Flow Diagrams

### 1. Login Flow

```
┌────────────────────┐
│   User Browser     │
│  (Load Login Page) │
└─────────┬──────────┘
          │
          ▼
┌────────────────────────────┐
│    Frontend (React)        │
│   Renders Login Form       │
└─────────┬──────────────────┘
          │ User enters username/password
          │ and clicks "Sign In"
          ▼
┌────────────────────────────┐
│   API Request (HTTP)       │
│  POST /auth/login          │
│  {username, password}      │
└─────────┬──────────────────┘
          │
          │ HTTPS to backend
          │
          ▼
┌────────────────────────────┐
│  Backend (FastAPI)         │
│  /auth/login endpoint      │
│  - Verify username exists  │
│  - Check password hash     │
│  - Generate JWT token     │
└─────────┬──────────────────┘
          │
          ▼
┌────────────────────────────┐
│   PostgreSQL Database      │
│  SELECT user WHERE...      │
│  (Verify credentials)      │
└─────────┬──────────────────┘
          │
          ▼
┌────────────────────────────┐
│  Backend Response (HTTP)   │
│  {                         │
│   "access_token": "JWT...", │
│   "token_type": "bearer",  │
│   "user": {                │
│    "id": "admin",          │
│    "username": "admin",    │
│    "role": "admin",        │
│    "teamId": null,         │
│    "teamName": null        │
│   }                        │
│  }                         │
└─────────┬──────────────────┘
          │
          ▼
┌────────────────────────────┐
│   Frontend (React)         │
│  - Store JWT in localStorage │
│  - Set user context        │
│  - Redirect to /admin      │
└─────────┬──────────────────┘
          │
          ▼
┌────────────────────────────┐
│  User Logged In ✅         │
│  Admin Dashboard Loaded    │
└────────────────────────────┘
```

### 2. Fetch Players Flow

```
┌─────────────────────┐
│  Admin Dashboard    │
│  Load Player List   │
└────────┬────────────┘
         │
         ▼
┌──────────────────────────────┐
│  API Request (HTTP + Auth)   │
│  GET /players                │
│  Header:                      │
│  Authorization: Bearer JWT... │
└────────┬─────────────────────┘
         │
         │ HTTPS to backend
         │
         ▼
┌──────────────────────────────┐
│  Backend (FastAPI)           │
│  /players endpoint           │
│  - Decode JWT token         │
│  - Verify user authenticated │
│  - Query database           │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  PostgreSQL Database         │
│  SELECT * FROM players      │
│  (Returns 53 players)        │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  Backend Response (HTTP)     │
│  [                           │
│   {                          │
│    "id": 1,                  │
│    "name": "Virat Kohli",   │
│    "role": "batsman",       │
│    "basePrice": 2000000,    │
│    "status": "available",   │
│    "image": "..."           │
│   },                        │
│   ... (53 total)            │
│  ]                           │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  Frontend (React)            │
│  - Store in Zustand store   │
│  - Render player list       │
│  - Update UI               │
└──────────────────────────────┘
```

### 3. WebSocket Real-Time Update Flow

```
┌────────────────────────────┐
│  Frontend (Connected via WS)│
│  wss://backend/ws/auction  │
└────────┬───────────────────┘
         │ (Persistent connection)
         │
         ▼
┌────────────────────────────┐
│  Backend WebSocket Handler │
│  /ws/auction endpoint      │
│  - Accept connection       │
│  - Add to active clients   │
└────────┬───────────────────┘
         │
    ┌────┴────┬────────┬──────────┐
    │          │        │          │
    ▼          ▼        ▼          ▼
  (Waiting for events...)
    │          │        │          │
┌───┴─────┐ ┌─┴─┐ ┌────┴───┐ ┌───┴──┐
│ Bid     │ │..│ │ Player │ │State │
│ Placed  │ │  │ │ Sold   │ │ Chng │
└────┬────┘ └─┬─┘ └────┬───┘ └───┬──┘
     │        │        │         │
     └────┬───┴────┬───┴─────┬───┘
          │        │         │
          ▼        ▼         ▼
┌────────────────────────────────────┐
│    Backend Broadcasts Message      │
│    To all connected WebSocket      │
│    clients                         │
└────────┬─────────────────────────┘
         │
    ┌────┴────┬──────────┬────────┐
    │          │          │        │
    ▼          ▼          ▼        ▼
┌─────────┐┌─────────┐┌─────────┐┌─────────┐
│Frontend1││Frontend2││Frontend3││Frontend4│
│ Browser ││ Browser ││ Browser ││ Browser │
└────┬────┘└────┬────┘└────┬────┘└────┬───┘
     │          │          │         │
     ▼          ▼          ▼         ▼
┌──────────────────────────────────┐
│   All clients receive update     │
│   in real-time (sub-second)     │
│   Zustand store updates         │
│   UI re-renders automatically   │
└──────────────────────────────────┘
```

---

## Component Interaction Map

```
Frontend (React Components)
│
├─ Login.tsx
│  └─ Calls: POST /auth/login
│      └─ Stores: JWT token + user info
│      └─ Navigates: /admin or /presenter or /viewer
│
├─ AdminPanel.tsx (Protected route)
│  ├─ Calls: GET /players (via dataService)
│  ├─ Calls: GET /teams (via dataService)
│  ├─ Calls: GET /auction/state (via dataService)
│  ├─ Calls: POST /auction/start
│  ├─ Calls: POST /auction/next
│  ├─ Calls: POST /auction/bid
│  ├─ Calls: POST /auction/mark-sold
│  └─ Updates: Zustand store on changes
│
├─ PresenterPanel.tsx
│  ├─ Same as AdminPanel
│  └─ Limited permissions
│
├─ ViewerScreen.tsx
│  ├─ Calls: GET /auction/state (polls)
│  ├─ Connects: WebSocket /ws/auction
│  └─ Display only (read-only)
│
└─ useAuctionStore.ts (Zustand State)
   ├─ players: Player[]
   ├─ teams: Team[]
   ├─ auctionStarted: boolean
   ├─ currentPlayer: Player | null
   ├─ currentBid: number
   ├─ bidHistory: Bid[]
   └─ Methods:
      ├─ startAuction()
      ├─ placeBid()
      ├─ markSold()
      └─ updateFromAPI()
```

---

## API Endpoint Summary

```
Authentication
├─ POST /auth/login
├─ POST /auth/logout
└─ GET /auth/profile

Players (CRUD)
├─ GET /players
├─ GET /players/{id}
├─ POST /players
├─ PUT /players/{id}
└─ DELETE /players/{id}

Teams
├─ GET /teams
├─ GET /teams/{id}
└─ PUT /teams/{id}/purse

Auction Control
├─ GET /auction/state
├─ POST /auction/start
├─ POST /auction/pause
├─ POST /auction/resume
├─ POST /auction/next
├─ POST /auction/previous
├─ POST /auction/bid
├─ POST /auction/mark-sold
├─ POST /auction/mark-unsold
└─ POST /auction/reset

Management (Admin only)
└─ POST /management/reset-all

WebSocket
└─ WS /ws/auction

Utilities
├─ GET / (root)
├─ GET /health
├─ GET /docs (Swagger)
└─ GET /redoc (ReDoc)
```

---

## Environment Configuration Flow

```
┌──────────────────────────┐
│  Render Dashboard        │
│  Environment Variables   │
└───────────────┬──────────┘
                │
                ▼
        ┌───────────────────┐
        │  Backend Service  │
        │  Start-up         │
        └───────────┬───────┘
                    │
        ┌───────────┴─────────────┐
        │                         │
        ▼                         ▼
    ┌───────────┐           ┌──────────────┐
    │ load_env  │           │ Fallback     │
    │ from      │           │ defaults     │
    │ Render    │           │ (if not set) │
    └─────┬─────┘           └──────┬───────┘
          │                        │
          ├────────────┬───────────┤
          │            │           │
   ┌──────▼──┐  ┌──────▼──┐  ┌────▼──────┐
   │ Passwords│  │ Secret  │  │ Database  │
   │          │  │ Key     │  │ URL       │
   └──────┬───┘  └────┬────┘  └────┬──────┘
          │           │            │
          └───────┬───┴─────┬──────┘
                  │         │
                  ▼         ▼
          ┌───────────────────────┐
          │ Backend Config        │
          │ app/config.py         │
          │ Variables available   │
          │ throughout app        │
          └───────────┬───────────┘
                      │
              ┌───────┴────────┐
              │                │
              ▼                ▼
        ┌──────────────┐  ┌──────────────┐
        │ Seed DB      │  │ JWT Config   │
        │ with users   │  │ (Token life) │
        └──────┬───────┘  └──────┬───────┘
               │                │
               └────────┬───────┘
                        │
                        ▼
            ┌─────────────────────┐
            │ Backend Ready! ✅    │
            │ Can authenticate    │
            │ users successfully  │
            └─────────────────────┘
```

---

## Deployment Stack

```
┌─────────────────────────────────────────────┐
│          Render.com Infrastructure          │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Frontend Web Service               │   │
│  │  (Node.js + Vite)                   │   │
│  │  URL: auction-portal-frontend...    │   │
│  │  Build: npm run build               │   │
│  │  Start: npm run preview             │   │
│  └──────────────┬──────────────────────┘   │
│                 │                          │
│                 │ HTTPS + CORS             │
│                 │                          │
│  ┌──────────────▼──────────────────────┐   │
│  │  Backend Web Service                │   │
│  │  (Python + FastAPI + Uvicorn)       │   │
│  │  URL: auction-portal-7bds...        │   │
│  │  Start: uvicorn app.main:app        │   │
│  └──────────────┬──────────────────────┘   │
│                 │                          │
│                 │ SQL                      │
│                 │                          │
│  ┌──────────────▼──────────────────────┐   │
│  │  PostgreSQL Database                │   │
│  │  (Neon Serverless)                  │   │
│  │  Managed by Render                  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  Environment Variables:                     │
│  ├─ All 14 password/key vars               │
│  ├─ DATABASE_URL (auto-set)                │
│  └─ NODE_ENV, PORT, etc                    │
│                                             │
└─────────────────────────────────────────────┘
```

---

## Request/Response Cycle Example

```
Browser User Action: Click "Place Bid"
│
▼
Frontend React Component captures action
│
▼
Call apiClient.post('/auction/bid', {teamId, amount})
│
▼
┌──────────────────────────────────────┐
│ HTTP POST Request                    │
│ URL: https://...backend.../bid       │
│ Headers: {                           │
│   "Content-Type": "application/json",│
│   "Authorization": "Bearer JWT..."   │
│ }                                    │
│ Body: {                              │
│   "teamId": 1,                       │
│   "amount": 500000                   │
│ }                                    │
└──────────────────────┬───────────────┘
                       │ 🌐 HTTPS (encrypted)
                       ▼
        ┌──────────────────────────────┐
        │ Render Backend receives      │
        │ - Parses JSON request        │
        │ - Validates authorization   │
        │ - Checks user permissions   │
        │ - Validates bid amount      │
        └──────────────┬───────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │ Update Database              │
        │ INSERT INTO bids (...)       │
        │ UPDATE auction_state (...)   │
        └──────────────┬───────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │ Broadcast via WebSocket      │
        │ Send to all connected        │
        │ clients: "bid placed"        │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
Response to Requesting       Update Other
Client (via HTTP)            Clients (via WS)
│                            │
▼                            ▼
┌────────────────┐    ┌──────────────────┐
│ HTTP 200 OK    │    │ WebSocket        │
│ {              │    │ Message          │
│  "success":    │    │ {                │
│    true,       │    │  "type":         │
│  "bid": {...}  │    │    "bid_placed", │
│ }              │    │  "data": {...}   │
└────────┬───────┘    │ }                │
         │            └────────┬─────────┘
         │                     │
         ▼                     ▼
   Frontend Updates       Frontend Updates
   Zustand Store          Zustand Store
         │                     │
         │                     │
         └──────────┬──────────┘
                    │
                    ▼
          React Components Re-render
          UI reflects new bid
          All clients show same state
                    │
                    ▼
            ✅ Success! Everyone sees
               the new bid in real-time
```

---

## Summary

✅ **Frontend** → Sends HTTP requests + maintains WebSocket
✅ **Backend** → Processes requests, manages state, broadcasts updates
✅ **Database** → Persists data, handles concurrent queries
✅ **WebSocket** → Real-time updates to all connected clients
✅ **HTTPS/WSS** → Secure encrypted communication
✅ **Environment** → Secrets managed via Render dashboard

**System is production-ready** (pending env var setup)
